#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:52:20.310172+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Smart Sensor Sampling

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 1"
  echo "  adc_resolution_bits = 16"
  echo "  averaging_window = 32"
  echo "  sleep_mode_depth = 1"
  echo "  wakeup_interval_sec = 1"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "1" \
    --adc_resolution_bits "16" \
    --averaging_window "32" \
    --sleep_mode_depth "1" \
    --wakeup_interval_sec "1" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 100"
  echo "  adc_resolution_bits = 8"
  echo "  averaging_window = 1"
  echo "  sleep_mode_depth = 1"
  echo "  wakeup_interval_sec = 1"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "100" \
    --adc_resolution_bits "8" \
    --averaging_window "1" \
    --sleep_mode_depth "1" \
    --wakeup_interval_sec "1" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 100"
  echo "  adc_resolution_bits = 16"
  echo "  averaging_window = 1"
  echo "  sleep_mode_depth = 4"
  echo "  wakeup_interval_sec = 1"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "100" \
    --adc_resolution_bits "16" \
    --averaging_window "1" \
    --sleep_mode_depth "4" \
    --wakeup_interval_sec "1" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 100"
  echo "  adc_resolution_bits = 16"
  echo "  averaging_window = 32"
  echo "  sleep_mode_depth = 4"
  echo "  wakeup_interval_sec = 60"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "100" \
    --adc_resolution_bits "16" \
    --averaging_window "32" \
    --sleep_mode_depth "4" \
    --wakeup_interval_sec "60" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 1"
  echo "  adc_resolution_bits = 16"
  echo "  averaging_window = 1"
  echo "  sleep_mode_depth = 1"
  echo "  wakeup_interval_sec = 60"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "1" \
    --adc_resolution_bits "16" \
    --averaging_window "1" \
    --sleep_mode_depth "1" \
    --wakeup_interval_sec "60" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 100"
  echo "  adc_resolution_bits = 8"
  echo "  averaging_window = 32"
  echo "  sleep_mode_depth = 1"
  echo "  wakeup_interval_sec = 60"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "100" \
    --adc_resolution_bits "8" \
    --averaging_window "32" \
    --sleep_mode_depth "1" \
    --wakeup_interval_sec "60" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 1"
  echo "  adc_resolution_bits = 8"
  echo "  averaging_window = 1"
  echo "  sleep_mode_depth = 4"
  echo "  wakeup_interval_sec = 60"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "1" \
    --adc_resolution_bits "8" \
    --averaging_window "1" \
    --sleep_mode_depth "4" \
    --wakeup_interval_sec "60" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  sample_rate_hz = 1"
  echo "  adc_resolution_bits = 8"
  echo "  averaging_window = 32"
  echo "  sleep_mode_depth = 4"
  echo "  wakeup_interval_sec = 1"


  if ! "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/sim.sh" \
    --sample_rate_hz "1" \
    --adc_resolution_bits "8" \
    --averaging_window "32" \
    --sleep_mode_depth "4" \
    --wakeup_interval_sec "1" \
    --mcu "esp32" \
    --sensor "bme280" \
    --out "/workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/67_smart_sensor_sampling/results"
