#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:51:04.348618+00:00
# Operation: full_factorial
# Total runs: 8
# Plan: Certificate Rotation Strategy

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 30"
  echo "  renewal_window_pct = 30"
  echo "  stapling_cache_sec = 3600"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "30" \
    --renewal_window_pct "30" \
    --stapling_cache_sec "3600" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 90"
  echo "  renewal_window_pct = 10"
  echo "  stapling_cache_sec = 300"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "90" \
    --renewal_window_pct "10" \
    --stapling_cache_sec "300" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 90"
  echo "  renewal_window_pct = 30"
  echo "  stapling_cache_sec = 300"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "90" \
    --renewal_window_pct "30" \
    --stapling_cache_sec "300" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 90"
  echo "  renewal_window_pct = 30"
  echo "  stapling_cache_sec = 3600"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "90" \
    --renewal_window_pct "30" \
    --stapling_cache_sec "3600" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 30"
  echo "  renewal_window_pct = 30"
  echo "  stapling_cache_sec = 300"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "30" \
    --renewal_window_pct "30" \
    --stapling_cache_sec "300" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 90"
  echo "  renewal_window_pct = 10"
  echo "  stapling_cache_sec = 3600"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "90" \
    --renewal_window_pct "10" \
    --stapling_cache_sec "3600" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 30"
  echo "  renewal_window_pct = 10"
  echo "  stapling_cache_sec = 300"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "30" \
    --renewal_window_pct "10" \
    --stapling_cache_sec "300" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  cert_lifetime_days = 30"
  echo "  renewal_window_pct = 10"
  echo "  stapling_cache_sec = 3600"


  if ! "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/sim.sh" \
    --cert_lifetime_days "30" \
    --renewal_window_pct "10" \
    --stapling_cache_sec "3600" \
    --ca "letsencrypt" \
    --key_type "ecdsa_p256" \
    --out "/workspaces/design_of_experiments/use_cases/62_certificate_rotation/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/62_certificate_rotation/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/62_certificate_rotation/results"
