#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:55:56.514263+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: Firewall Rule Ordering

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  rule_count = 5000"
  echo "  conntrack_max = 1048576"
  echo "  rule_ordering = sequential"
  echo "  hashlimit_burst = 5"
  echo "  nf_tables = iptables"
  echo "  batch_verdict = off"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "5000" \
    --conntrack_max "1048576" \
    --rule_ordering "sequential" \
    --hashlimit_burst "5" \
    --nf_tables "iptables" \
    --batch_verdict "off" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  rule_count = 100"
  echo "  conntrack_max = 65536"
  echo "  rule_ordering = sequential"
  echo "  hashlimit_burst = 100"
  echo "  nf_tables = iptables"
  echo "  batch_verdict = off"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "100" \
    --conntrack_max "65536" \
    --rule_ordering "sequential" \
    --hashlimit_burst "100" \
    --nf_tables "iptables" \
    --batch_verdict "off" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  rule_count = 100"
  echo "  conntrack_max = 1048576"
  echo "  rule_ordering = frequency"
  echo "  hashlimit_burst = 100"
  echo "  nf_tables = iptables"
  echo "  batch_verdict = on"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "100" \
    --conntrack_max "1048576" \
    --rule_ordering "frequency" \
    --hashlimit_burst "100" \
    --nf_tables "iptables" \
    --batch_verdict "on" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  rule_count = 5000"
  echo "  conntrack_max = 1048576"
  echo "  rule_ordering = sequential"
  echo "  hashlimit_burst = 100"
  echo "  nf_tables = nftables"
  echo "  batch_verdict = on"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "5000" \
    --conntrack_max "1048576" \
    --rule_ordering "sequential" \
    --hashlimit_burst "100" \
    --nf_tables "nftables" \
    --batch_verdict "on" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  rule_count = 100"
  echo "  conntrack_max = 1048576"
  echo "  rule_ordering = frequency"
  echo "  hashlimit_burst = 5"
  echo "  nf_tables = nftables"
  echo "  batch_verdict = off"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "100" \
    --conntrack_max "1048576" \
    --rule_ordering "frequency" \
    --hashlimit_burst "5" \
    --nf_tables "nftables" \
    --batch_verdict "off" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  rule_count = 5000"
  echo "  conntrack_max = 65536"
  echo "  rule_ordering = frequency"
  echo "  hashlimit_burst = 100"
  echo "  nf_tables = nftables"
  echo "  batch_verdict = off"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "5000" \
    --conntrack_max "65536" \
    --rule_ordering "frequency" \
    --hashlimit_burst "100" \
    --nf_tables "nftables" \
    --batch_verdict "off" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  rule_count = 100"
  echo "  conntrack_max = 65536"
  echo "  rule_ordering = sequential"
  echo "  hashlimit_burst = 5"
  echo "  nf_tables = nftables"
  echo "  batch_verdict = on"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "100" \
    --conntrack_max "65536" \
    --rule_ordering "sequential" \
    --hashlimit_burst "5" \
    --nf_tables "nftables" \
    --batch_verdict "on" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  rule_count = 5000"
  echo "  conntrack_max = 65536"
  echo "  rule_ordering = frequency"
  echo "  hashlimit_burst = 5"
  echo "  nf_tables = iptables"
  echo "  batch_verdict = on"


  if ! "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/sim.sh" \
    --rule_count "5000" \
    --conntrack_max "65536" \
    --rule_ordering "frequency" \
    --hashlimit_burst "5" \
    --nf_tables "iptables" \
    --batch_verdict "on" \
    --interface "eth0" \
    --protocol_mix "80_tcp_20_udp" \
    --out "/workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/49_firewall_rule_ordering/results"
