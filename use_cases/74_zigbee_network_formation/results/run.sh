#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:54:26.101174+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Zigbee Network Formation

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 2"
  echo "  max_children = 20"
  echo "  link_cost_threshold = 7"
  echo "  route_table_size = 10"
  echo "  poll_rate_ms = 100"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "2" \
    --max_children "20" \
    --link_cost_threshold "7" \
    --route_table_size "10" \
    --poll_rate_ms "100" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 7"
  echo "  max_children = 4"
  echo "  link_cost_threshold = 1"
  echo "  route_table_size = 10"
  echo "  poll_rate_ms = 100"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "7" \
    --max_children "4" \
    --link_cost_threshold "1" \
    --route_table_size "10" \
    --poll_rate_ms "100" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 7"
  echo "  max_children = 20"
  echo "  link_cost_threshold = 1"
  echo "  route_table_size = 50"
  echo "  poll_rate_ms = 100"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "7" \
    --max_children "20" \
    --link_cost_threshold "1" \
    --route_table_size "50" \
    --poll_rate_ms "100" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 7"
  echo "  max_children = 20"
  echo "  link_cost_threshold = 7"
  echo "  route_table_size = 50"
  echo "  poll_rate_ms = 2000"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "7" \
    --max_children "20" \
    --link_cost_threshold "7" \
    --route_table_size "50" \
    --poll_rate_ms "2000" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 2"
  echo "  max_children = 20"
  echo "  link_cost_threshold = 1"
  echo "  route_table_size = 10"
  echo "  poll_rate_ms = 2000"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "2" \
    --max_children "20" \
    --link_cost_threshold "1" \
    --route_table_size "10" \
    --poll_rate_ms "2000" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 7"
  echo "  max_children = 4"
  echo "  link_cost_threshold = 7"
  echo "  route_table_size = 10"
  echo "  poll_rate_ms = 2000"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "7" \
    --max_children "4" \
    --link_cost_threshold "7" \
    --route_table_size "10" \
    --poll_rate_ms "2000" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 2"
  echo "  max_children = 4"
  echo "  link_cost_threshold = 1"
  echo "  route_table_size = 50"
  echo "  poll_rate_ms = 2000"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "2" \
    --max_children "4" \
    --link_cost_threshold "1" \
    --route_table_size "50" \
    --poll_rate_ms "2000" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  scan_duration_exp = 2"
  echo "  max_children = 4"
  echo "  link_cost_threshold = 7"
  echo "  route_table_size = 50"
  echo "  poll_rate_ms = 100"


  if ! "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/sim.sh" \
    --scan_duration_exp "2" \
    --max_children "4" \
    --link_cost_threshold "7" \
    --route_table_size "50" \
    --poll_rate_ms "100" \
    --zigbee_stack "z_stack" \
    --channel "15" \
    --out "/workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/74_zigbee_network_formation/results"
