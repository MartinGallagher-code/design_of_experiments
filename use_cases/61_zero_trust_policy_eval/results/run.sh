#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:50:42.856477+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Zero Trust Policy Evaluation

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 10"
  echo "  context_attributes = 12"
  echo "  risk_score_weight = 0.9"
  echo "  session_timeout = 300"
  echo "  mfa_frequency = 1"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "10" \
    --context_attributes "12" \
    --risk_score_weight "0.9" \
    --session_timeout "300" \
    --mfa_frequency "1" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 300"
  echo "  context_attributes = 3"
  echo "  risk_score_weight = 0.1"
  echo "  session_timeout = 300"
  echo "  mfa_frequency = 1"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "300" \
    --context_attributes "3" \
    --risk_score_weight "0.1" \
    --session_timeout "300" \
    --mfa_frequency "1" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 300"
  echo "  context_attributes = 12"
  echo "  risk_score_weight = 0.1"
  echo "  session_timeout = 3600"
  echo "  mfa_frequency = 1"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "300" \
    --context_attributes "12" \
    --risk_score_weight "0.1" \
    --session_timeout "3600" \
    --mfa_frequency "1" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 300"
  echo "  context_attributes = 12"
  echo "  risk_score_weight = 0.9"
  echo "  session_timeout = 3600"
  echo "  mfa_frequency = 24"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "300" \
    --context_attributes "12" \
    --risk_score_weight "0.9" \
    --session_timeout "3600" \
    --mfa_frequency "24" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 10"
  echo "  context_attributes = 12"
  echo "  risk_score_weight = 0.1"
  echo "  session_timeout = 300"
  echo "  mfa_frequency = 24"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "10" \
    --context_attributes "12" \
    --risk_score_weight "0.1" \
    --session_timeout "300" \
    --mfa_frequency "24" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 300"
  echo "  context_attributes = 3"
  echo "  risk_score_weight = 0.9"
  echo "  session_timeout = 300"
  echo "  mfa_frequency = 24"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "300" \
    --context_attributes "3" \
    --risk_score_weight "0.9" \
    --session_timeout "300" \
    --mfa_frequency "24" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 10"
  echo "  context_attributes = 3"
  echo "  risk_score_weight = 0.1"
  echo "  session_timeout = 3600"
  echo "  mfa_frequency = 24"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "10" \
    --context_attributes "3" \
    --risk_score_weight "0.1" \
    --session_timeout "3600" \
    --mfa_frequency "24" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  policy_cache_ttl = 10"
  echo "  context_attributes = 3"
  echo "  risk_score_weight = 0.9"
  echo "  session_timeout = 3600"
  echo "  mfa_frequency = 1"


  if ! "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/sim.sh" \
    --policy_cache_ttl "10" \
    --context_attributes "3" \
    --risk_score_weight "0.9" \
    --session_timeout "3600" \
    --mfa_frequency "1" \
    --identity_provider "okta" \
    --policy_engine "opa" \
    --out "/workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/61_zero_trust_policy_eval/results"
