#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:50:43.418533+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: Serverless Cold Start

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  memory_mb = 1024"
  echo "  runtime = go"
  echo "  package_mb = 50"
  echo "  vpc_enabled = no"
  echo "  layers_count = 0"
  echo "  provisioned = 0"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "1024" \
    --runtime "go" \
    --package_mb "50" \
    --vpc_enabled "no" \
    --layers_count "0" \
    --provisioned "0" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  memory_mb = 128"
  echo "  runtime = python"
  echo "  package_mb = 50"
  echo "  vpc_enabled = yes"
  echo "  layers_count = 0"
  echo "  provisioned = 0"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "128" \
    --runtime "python" \
    --package_mb "50" \
    --vpc_enabled "yes" \
    --layers_count "0" \
    --provisioned "0" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  memory_mb = 128"
  echo "  runtime = go"
  echo "  package_mb = 5"
  echo "  vpc_enabled = yes"
  echo "  layers_count = 0"
  echo "  provisioned = 10"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "128" \
    --runtime "go" \
    --package_mb "5" \
    --vpc_enabled "yes" \
    --layers_count "0" \
    --provisioned "10" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  memory_mb = 1024"
  echo "  runtime = go"
  echo "  package_mb = 50"
  echo "  vpc_enabled = yes"
  echo "  layers_count = 5"
  echo "  provisioned = 10"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "1024" \
    --runtime "go" \
    --package_mb "50" \
    --vpc_enabled "yes" \
    --layers_count "5" \
    --provisioned "10" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  memory_mb = 128"
  echo "  runtime = go"
  echo "  package_mb = 5"
  echo "  vpc_enabled = no"
  echo "  layers_count = 5"
  echo "  provisioned = 0"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "128" \
    --runtime "go" \
    --package_mb "5" \
    --vpc_enabled "no" \
    --layers_count "5" \
    --provisioned "0" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  memory_mb = 1024"
  echo "  runtime = python"
  echo "  package_mb = 5"
  echo "  vpc_enabled = yes"
  echo "  layers_count = 5"
  echo "  provisioned = 0"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "1024" \
    --runtime "python" \
    --package_mb "5" \
    --vpc_enabled "yes" \
    --layers_count "5" \
    --provisioned "0" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  memory_mb = 128"
  echo "  runtime = python"
  echo "  package_mb = 50"
  echo "  vpc_enabled = no"
  echo "  layers_count = 5"
  echo "  provisioned = 10"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "128" \
    --runtime "python" \
    --package_mb "50" \
    --vpc_enabled "no" \
    --layers_count "5" \
    --provisioned "10" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  memory_mb = 1024"
  echo "  runtime = python"
  echo "  package_mb = 5"
  echo "  vpc_enabled = no"
  echo "  layers_count = 0"
  echo "  provisioned = 10"


  if ! "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/sim.sh" \
    --memory_mb "1024" \
    --runtime "python" \
    --package_mb "5" \
    --vpc_enabled "no" \
    --layers_count "0" \
    --provisioned "10" \
    --region "us-east-1" \
    --trigger "api-gateway" \
    --out "/workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/30_serverless_cold_start/results"
