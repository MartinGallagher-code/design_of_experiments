#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:55:30.159542+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: Terraform Plan Optimization

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  parallelism = 20"
  echo "  refresh_enabled = on"
  echo "  state_lock_timeout = 120"
  echo "  provider_cache = off"
  echo "  plan_out_format = text"
  echo "  detailed_exitcode = off"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "20" \
    --refresh_enabled "on" \
    --state_lock_timeout "120" \
    --provider_cache "off" \
    --plan_out_format "text" \
    --detailed_exitcode "off" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  parallelism = 1"
  echo "  refresh_enabled = off"
  echo "  state_lock_timeout = 120"
  echo "  provider_cache = on"
  echo "  plan_out_format = text"
  echo "  detailed_exitcode = off"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "1" \
    --refresh_enabled "off" \
    --state_lock_timeout "120" \
    --provider_cache "on" \
    --plan_out_format "text" \
    --detailed_exitcode "off" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  parallelism = 1"
  echo "  refresh_enabled = on"
  echo "  state_lock_timeout = 5"
  echo "  provider_cache = on"
  echo "  plan_out_format = text"
  echo "  detailed_exitcode = on"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "1" \
    --refresh_enabled "on" \
    --state_lock_timeout "5" \
    --provider_cache "on" \
    --plan_out_format "text" \
    --detailed_exitcode "on" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  parallelism = 20"
  echo "  refresh_enabled = on"
  echo "  state_lock_timeout = 120"
  echo "  provider_cache = on"
  echo "  plan_out_format = json"
  echo "  detailed_exitcode = on"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "20" \
    --refresh_enabled "on" \
    --state_lock_timeout "120" \
    --provider_cache "on" \
    --plan_out_format "json" \
    --detailed_exitcode "on" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  parallelism = 1"
  echo "  refresh_enabled = on"
  echo "  state_lock_timeout = 5"
  echo "  provider_cache = off"
  echo "  plan_out_format = json"
  echo "  detailed_exitcode = off"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "1" \
    --refresh_enabled "on" \
    --state_lock_timeout "5" \
    --provider_cache "off" \
    --plan_out_format "json" \
    --detailed_exitcode "off" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  parallelism = 20"
  echo "  refresh_enabled = off"
  echo "  state_lock_timeout = 5"
  echo "  provider_cache = on"
  echo "  plan_out_format = json"
  echo "  detailed_exitcode = off"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "20" \
    --refresh_enabled "off" \
    --state_lock_timeout "5" \
    --provider_cache "on" \
    --plan_out_format "json" \
    --detailed_exitcode "off" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  parallelism = 1"
  echo "  refresh_enabled = off"
  echo "  state_lock_timeout = 120"
  echo "  provider_cache = off"
  echo "  plan_out_format = json"
  echo "  detailed_exitcode = on"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "1" \
    --refresh_enabled "off" \
    --state_lock_timeout "120" \
    --provider_cache "off" \
    --plan_out_format "json" \
    --detailed_exitcode "on" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  parallelism = 20"
  echo "  refresh_enabled = off"
  echo "  state_lock_timeout = 5"
  echo "  provider_cache = off"
  echo "  plan_out_format = text"
  echo "  detailed_exitcode = on"


  if ! "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/sim.sh" \
    --parallelism "20" \
    --refresh_enabled "off" \
    --state_lock_timeout "5" \
    --provider_cache "off" \
    --plan_out_format "text" \
    --detailed_exitcode "on" \
    --backend "s3" \
    --provider "aws" \
    --out "/workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/79_terraform_plan_optimization/results"
