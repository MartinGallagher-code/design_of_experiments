#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T17:19:12.018835+00:00
# Operation: full_factorial
# Total runs: 8
# Plan: NUMA Memory Placement

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  mem_policy = local"
  echo "  thread_bind = spread"
  echo "  hugepages = 2M"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "local" \
    --thread_bind "spread" \
    --hugepages "2M" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  mem_policy = interleave"
  echo "  thread_bind = close"
  echo "  hugepages = off"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "interleave" \
    --thread_bind "close" \
    --hugepages "off" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  mem_policy = interleave"
  echo "  thread_bind = spread"
  echo "  hugepages = off"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "interleave" \
    --thread_bind "spread" \
    --hugepages "off" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  mem_policy = interleave"
  echo "  thread_bind = spread"
  echo "  hugepages = 2M"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "interleave" \
    --thread_bind "spread" \
    --hugepages "2M" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  mem_policy = local"
  echo "  thread_bind = spread"
  echo "  hugepages = off"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "local" \
    --thread_bind "spread" \
    --hugepages "off" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  mem_policy = interleave"
  echo "  thread_bind = close"
  echo "  hugepages = 2M"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "interleave" \
    --thread_bind "close" \
    --hugepages "2M" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  mem_policy = local"
  echo "  thread_bind = close"
  echo "  hugepages = off"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "local" \
    --thread_bind "close" \
    --hugepages "off" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  mem_policy = local"
  echo "  thread_bind = close"
  echo "  hugepages = 2M"


  if ! "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/sim.sh" \
    --mem_policy "local" \
    --thread_bind "close" \
    --hugepages "2M" \
    --sockets "2" \
    --cores_per_socket "32" \
    --out "/workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/10_numa_memory_placement/results"
