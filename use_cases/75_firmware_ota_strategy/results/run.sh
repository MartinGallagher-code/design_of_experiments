#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:54:50.237292+00:00
# Operation: full_factorial
# Total runs: 8
# Plan: Firmware OTA Strategy

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 1"
  echo "  retry_count = 5"
  echo "  delta_encoding = on"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "1" \
    --retry_count "5" \
    --delta_encoding "on" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 64"
  echo "  retry_count = 1"
  echo "  delta_encoding = off"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "64" \
    --retry_count "1" \
    --delta_encoding "off" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 64"
  echo "  retry_count = 5"
  echo "  delta_encoding = off"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "64" \
    --retry_count "5" \
    --delta_encoding "off" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 64"
  echo "  retry_count = 5"
  echo "  delta_encoding = on"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "64" \
    --retry_count "5" \
    --delta_encoding "on" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 1"
  echo "  retry_count = 5"
  echo "  delta_encoding = off"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "1" \
    --retry_count "5" \
    --delta_encoding "off" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 64"
  echo "  retry_count = 1"
  echo "  delta_encoding = on"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "64" \
    --retry_count "1" \
    --delta_encoding "on" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 1"
  echo "  retry_count = 1"
  echo "  delta_encoding = off"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "1" \
    --retry_count "1" \
    --delta_encoding "off" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  chunk_size_kb = 1"
  echo "  retry_count = 1"
  echo "  delta_encoding = on"


  if ! "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/sim.sh" \
    --chunk_size_kb "1" \
    --retry_count "1" \
    --delta_encoding "on" \
    --transport "coap" \
    --compression "lz4" \
    --out "/workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/75_firmware_ota_strategy/results"
