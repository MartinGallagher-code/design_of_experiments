#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:56:34.153654+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Incident Response Automation

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 1"
  echo "  escalation_delay_min = 15"
  echo "  auto_remediation_enabled = on"
  echo "  runbook_timeout_sec = 30"
  echo "  notification_channels = 1"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "1" \
    --escalation_delay_min "15" \
    --auto_remediation_enabled "on" \
    --runbook_timeout_sec "30" \
    --notification_channels "1" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 5"
  echo "  escalation_delay_min = 1"
  echo "  auto_remediation_enabled = off"
  echo "  runbook_timeout_sec = 30"
  echo "  notification_channels = 1"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "5" \
    --escalation_delay_min "1" \
    --auto_remediation_enabled "off" \
    --runbook_timeout_sec "30" \
    --notification_channels "1" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 5"
  echo "  escalation_delay_min = 15"
  echo "  auto_remediation_enabled = off"
  echo "  runbook_timeout_sec = 300"
  echo "  notification_channels = 1"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "5" \
    --escalation_delay_min "15" \
    --auto_remediation_enabled "off" \
    --runbook_timeout_sec "300" \
    --notification_channels "1" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 5"
  echo "  escalation_delay_min = 15"
  echo "  auto_remediation_enabled = on"
  echo "  runbook_timeout_sec = 300"
  echo "  notification_channels = 5"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "5" \
    --escalation_delay_min "15" \
    --auto_remediation_enabled "on" \
    --runbook_timeout_sec "300" \
    --notification_channels "5" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 1"
  echo "  escalation_delay_min = 15"
  echo "  auto_remediation_enabled = off"
  echo "  runbook_timeout_sec = 30"
  echo "  notification_channels = 5"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "1" \
    --escalation_delay_min "15" \
    --auto_remediation_enabled "off" \
    --runbook_timeout_sec "30" \
    --notification_channels "5" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 5"
  echo "  escalation_delay_min = 1"
  echo "  auto_remediation_enabled = on"
  echo "  runbook_timeout_sec = 30"
  echo "  notification_channels = 5"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "5" \
    --escalation_delay_min "1" \
    --auto_remediation_enabled "on" \
    --runbook_timeout_sec "30" \
    --notification_channels "5" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 1"
  echo "  escalation_delay_min = 1"
  echo "  auto_remediation_enabled = off"
  echo "  runbook_timeout_sec = 300"
  echo "  notification_channels = 5"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "1" \
    --escalation_delay_min "1" \
    --auto_remediation_enabled "off" \
    --runbook_timeout_sec "300" \
    --notification_channels "5" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  alert_threshold_severity = 1"
  echo "  escalation_delay_min = 1"
  echo "  auto_remediation_enabled = on"
  echo "  runbook_timeout_sec = 300"
  echo "  notification_channels = 1"


  if ! "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/sim.sh" \
    --alert_threshold_severity "1" \
    --escalation_delay_min "1" \
    --auto_remediation_enabled "on" \
    --runbook_timeout_sec "300" \
    --notification_channels "1" \
    --platform "pagerduty" \
    --integration "slack" \
    --out "/workspaces/design_of_experiments/use_cases/85_incident_response_automation/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/85_incident_response_automation/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/85_incident_response_automation/results"
