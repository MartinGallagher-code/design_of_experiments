#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:55:38.427666+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Docker Build Layer Caching

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  build_cache_mode = inline"
  echo "  max_layers = 30"
  echo "  squash_enabled = on"
  echo "  build_arg_count = 0"
  echo "  multistage_stages = 1"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "inline" \
    --max_layers "30" \
    --squash_enabled "on" \
    --build_arg_count "0" \
    --multistage_stages "1" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  build_cache_mode = registry"
  echo "  max_layers = 5"
  echo "  squash_enabled = off"
  echo "  build_arg_count = 0"
  echo "  multistage_stages = 1"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "registry" \
    --max_layers "5" \
    --squash_enabled "off" \
    --build_arg_count "0" \
    --multistage_stages "1" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  build_cache_mode = registry"
  echo "  max_layers = 30"
  echo "  squash_enabled = off"
  echo "  build_arg_count = 10"
  echo "  multistage_stages = 1"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "registry" \
    --max_layers "30" \
    --squash_enabled "off" \
    --build_arg_count "10" \
    --multistage_stages "1" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  build_cache_mode = registry"
  echo "  max_layers = 30"
  echo "  squash_enabled = on"
  echo "  build_arg_count = 10"
  echo "  multistage_stages = 5"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "registry" \
    --max_layers "30" \
    --squash_enabled "on" \
    --build_arg_count "10" \
    --multistage_stages "5" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  build_cache_mode = inline"
  echo "  max_layers = 30"
  echo "  squash_enabled = off"
  echo "  build_arg_count = 0"
  echo "  multistage_stages = 5"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "inline" \
    --max_layers "30" \
    --squash_enabled "off" \
    --build_arg_count "0" \
    --multistage_stages "5" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  build_cache_mode = registry"
  echo "  max_layers = 5"
  echo "  squash_enabled = on"
  echo "  build_arg_count = 0"
  echo "  multistage_stages = 5"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "registry" \
    --max_layers "5" \
    --squash_enabled "on" \
    --build_arg_count "0" \
    --multistage_stages "5" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  build_cache_mode = inline"
  echo "  max_layers = 5"
  echo "  squash_enabled = off"
  echo "  build_arg_count = 10"
  echo "  multistage_stages = 5"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "inline" \
    --max_layers "5" \
    --squash_enabled "off" \
    --build_arg_count "10" \
    --multistage_stages "5" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  build_cache_mode = inline"
  echo "  max_layers = 5"
  echo "  squash_enabled = on"
  echo "  build_arg_count = 10"
  echo "  multistage_stages = 1"


  if ! "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/sim.sh" \
    --build_cache_mode "inline" \
    --max_layers "5" \
    --squash_enabled "on" \
    --build_arg_count "10" \
    --multistage_stages "1" \
    --builder "buildkit" \
    --registry "ecr" \
    --out "/workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/80_docker_build_layer_caching/results"
