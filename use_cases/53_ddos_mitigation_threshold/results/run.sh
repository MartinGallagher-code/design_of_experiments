#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:57:03.323864+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: DDoS Mitigation Threshold

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 50000"
  echo "  connection_limit = 10000"
  echo "  challenge_mode = javascript"
  echo "  geo_blocking = off"
  echo "  anomaly_window_s = 10"
  echo "  whitelist_size = 100"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "50000" \
    --connection_limit "10000" \
    --challenge_mode "javascript" \
    --geo_blocking "off" \
    --anomaly_window_s "10" \
    --whitelist_size "100" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 1000"
  echo "  connection_limit = 100"
  echo "  challenge_mode = javascript"
  echo "  geo_blocking = on"
  echo "  anomaly_window_s = 10"
  echo "  whitelist_size = 100"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "1000" \
    --connection_limit "100" \
    --challenge_mode "javascript" \
    --geo_blocking "on" \
    --anomaly_window_s "10" \
    --whitelist_size "100" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 1000"
  echo "  connection_limit = 10000"
  echo "  challenge_mode = off"
  echo "  geo_blocking = on"
  echo "  anomaly_window_s = 10"
  echo "  whitelist_size = 10000"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "1000" \
    --connection_limit "10000" \
    --challenge_mode "off" \
    --geo_blocking "on" \
    --anomaly_window_s "10" \
    --whitelist_size "10000" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 50000"
  echo "  connection_limit = 10000"
  echo "  challenge_mode = javascript"
  echo "  geo_blocking = on"
  echo "  anomaly_window_s = 300"
  echo "  whitelist_size = 10000"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "50000" \
    --connection_limit "10000" \
    --challenge_mode "javascript" \
    --geo_blocking "on" \
    --anomaly_window_s "300" \
    --whitelist_size "10000" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 1000"
  echo "  connection_limit = 10000"
  echo "  challenge_mode = off"
  echo "  geo_blocking = off"
  echo "  anomaly_window_s = 300"
  echo "  whitelist_size = 100"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "1000" \
    --connection_limit "10000" \
    --challenge_mode "off" \
    --geo_blocking "off" \
    --anomaly_window_s "300" \
    --whitelist_size "100" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 50000"
  echo "  connection_limit = 100"
  echo "  challenge_mode = off"
  echo "  geo_blocking = on"
  echo "  anomaly_window_s = 300"
  echo "  whitelist_size = 100"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "50000" \
    --connection_limit "100" \
    --challenge_mode "off" \
    --geo_blocking "on" \
    --anomaly_window_s "300" \
    --whitelist_size "100" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 1000"
  echo "  connection_limit = 100"
  echo "  challenge_mode = javascript"
  echo "  geo_blocking = off"
  echo "  anomaly_window_s = 300"
  echo "  whitelist_size = 10000"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "1000" \
    --connection_limit "100" \
    --challenge_mode "javascript" \
    --geo_blocking "off" \
    --anomaly_window_s "300" \
    --whitelist_size "10000" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  syn_rate_limit = 50000"
  echo "  connection_limit = 100"
  echo "  challenge_mode = off"
  echo "  geo_blocking = off"
  echo "  anomaly_window_s = 10"
  echo "  whitelist_size = 10000"


  if ! "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/sim.sh" \
    --syn_rate_limit "50000" \
    --connection_limit "100" \
    --challenge_mode "off" \
    --geo_blocking "off" \
    --anomaly_window_s "10" \
    --whitelist_size "10000" \
    --mitigation_provider "in_house" \
    --baseline_traffic "5000_rps" \
    --out "/workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/53_ddos_mitigation_threshold/results"
