#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T17:19:19.255819+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: Compiler Optimization Flags Screening

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  opt_level = O3"
  echo "  vectorize = avx512"
  echo "  lto = on"
  echo "  march = native"
  echo "  unroll = off"
  echo "  fast_math = off"
  echo "  pgo = off"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O3" \
    --vectorize "avx512" \
    --lto "on" \
    --march "native" \
    --unroll "off" \
    --fast_math "off" \
    --pgo "off" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  opt_level = O2"
  echo "  vectorize = off"
  echo "  lto = on"
  echo "  march = znver3"
  echo "  unroll = off"
  echo "  fast_math = off"
  echo "  pgo = on"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O2" \
    --vectorize "off" \
    --lto "on" \
    --march "znver3" \
    --unroll "off" \
    --fast_math "off" \
    --pgo "on" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  opt_level = O2"
  echo "  vectorize = avx512"
  echo "  lto = off"
  echo "  march = znver3"
  echo "  unroll = off"
  echo "  fast_math = on"
  echo "  pgo = off"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O2" \
    --vectorize "avx512" \
    --lto "off" \
    --march "znver3" \
    --unroll "off" \
    --fast_math "on" \
    --pgo "off" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  opt_level = O3"
  echo "  vectorize = avx512"
  echo "  lto = on"
  echo "  march = znver3"
  echo "  unroll = on"
  echo "  fast_math = on"
  echo "  pgo = on"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O3" \
    --vectorize "avx512" \
    --lto "on" \
    --march "znver3" \
    --unroll "on" \
    --fast_math "on" \
    --pgo "on" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  opt_level = O2"
  echo "  vectorize = avx512"
  echo "  lto = off"
  echo "  march = native"
  echo "  unroll = on"
  echo "  fast_math = off"
  echo "  pgo = on"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O2" \
    --vectorize "avx512" \
    --lto "off" \
    --march "native" \
    --unroll "on" \
    --fast_math "off" \
    --pgo "on" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  opt_level = O3"
  echo "  vectorize = off"
  echo "  lto = off"
  echo "  march = znver3"
  echo "  unroll = on"
  echo "  fast_math = off"
  echo "  pgo = off"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O3" \
    --vectorize "off" \
    --lto "off" \
    --march "znver3" \
    --unroll "on" \
    --fast_math "off" \
    --pgo "off" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  opt_level = O2"
  echo "  vectorize = off"
  echo "  lto = on"
  echo "  march = native"
  echo "  unroll = on"
  echo "  fast_math = on"
  echo "  pgo = off"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O2" \
    --vectorize "off" \
    --lto "on" \
    --march "native" \
    --unroll "on" \
    --fast_math "on" \
    --pgo "off" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  opt_level = O3"
  echo "  vectorize = off"
  echo "  lto = off"
  echo "  march = native"
  echo "  unroll = off"
  echo "  fast_math = on"
  echo "  pgo = on"


  if ! "/workspaces/design_of_experiments/use_cases/13_compiler_flags/sim.sh" \
    --opt_level "O3" \
    --vectorize "off" \
    --lto "off" \
    --march "native" \
    --unroll "off" \
    --fast_math "on" \
    --pgo "on" \
    --out "/workspaces/design_of_experiments/use_cases/13_compiler_flags/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/13_compiler_flags/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/13_compiler_flags/results"
