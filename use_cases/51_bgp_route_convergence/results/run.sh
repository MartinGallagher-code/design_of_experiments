#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:56:31.276928+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: BGP Route Convergence

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  keepalive_s = 10"
  echo "  hold_time_s = 180"
  echo "  mrai_s = 30"
  echo "  dampening = off"
  echo "  bfd = off"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "10" \
    --hold_time_s "180" \
    --mrai_s "30" \
    --dampening "off" \
    --bfd "off" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  keepalive_s = 60"
  echo "  hold_time_s = 30"
  echo "  mrai_s = 5"
  echo "  dampening = off"
  echo "  bfd = off"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "60" \
    --hold_time_s "30" \
    --mrai_s "5" \
    --dampening "off" \
    --bfd "off" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  keepalive_s = 60"
  echo "  hold_time_s = 180"
  echo "  mrai_s = 5"
  echo "  dampening = on"
  echo "  bfd = off"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "60" \
    --hold_time_s "180" \
    --mrai_s "5" \
    --dampening "on" \
    --bfd "off" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  keepalive_s = 60"
  echo "  hold_time_s = 180"
  echo "  mrai_s = 30"
  echo "  dampening = on"
  echo "  bfd = on"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "60" \
    --hold_time_s "180" \
    --mrai_s "30" \
    --dampening "on" \
    --bfd "on" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  keepalive_s = 10"
  echo "  hold_time_s = 180"
  echo "  mrai_s = 5"
  echo "  dampening = off"
  echo "  bfd = on"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "10" \
    --hold_time_s "180" \
    --mrai_s "5" \
    --dampening "off" \
    --bfd "on" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  keepalive_s = 60"
  echo "  hold_time_s = 30"
  echo "  mrai_s = 30"
  echo "  dampening = off"
  echo "  bfd = on"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "60" \
    --hold_time_s "30" \
    --mrai_s "30" \
    --dampening "off" \
    --bfd "on" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  keepalive_s = 10"
  echo "  hold_time_s = 30"
  echo "  mrai_s = 5"
  echo "  dampening = on"
  echo "  bfd = on"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "10" \
    --hold_time_s "30" \
    --mrai_s "5" \
    --dampening "on" \
    --bfd "on" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  keepalive_s = 10"
  echo "  hold_time_s = 30"
  echo "  mrai_s = 30"
  echo "  dampening = on"
  echo "  bfd = off"


  if ! "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/sim.sh" \
    --keepalive_s "10" \
    --hold_time_s "30" \
    --mrai_s "30" \
    --dampening "on" \
    --bfd "off" \
    --as_count "50" \
    --topology "partial_mesh" \
    --out "/workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/51_bgp_route_convergence/results"
