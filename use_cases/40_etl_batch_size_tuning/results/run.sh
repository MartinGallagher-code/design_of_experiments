#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:53:15.131697+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: ETL Batch Size Tuning

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  batch_size = 1000"
  echo "  writer_threads = 16"
  echo "  commit_interval = 50000"
  echo "  transform_mode = row"
  echo "  buffer_mb = 64"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "1000" \
    --writer_threads "16" \
    --commit_interval "50000" \
    --transform_mode "row" \
    --buffer_mb "64" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  batch_size = 100000"
  echo "  writer_threads = 1"
  echo "  commit_interval = 1000"
  echo "  transform_mode = row"
  echo "  buffer_mb = 64"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "100000" \
    --writer_threads "1" \
    --commit_interval "1000" \
    --transform_mode "row" \
    --buffer_mb "64" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  batch_size = 100000"
  echo "  writer_threads = 16"
  echo "  commit_interval = 1000"
  echo "  transform_mode = vectorized"
  echo "  buffer_mb = 64"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "100000" \
    --writer_threads "16" \
    --commit_interval "1000" \
    --transform_mode "vectorized" \
    --buffer_mb "64" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  batch_size = 100000"
  echo "  writer_threads = 16"
  echo "  commit_interval = 50000"
  echo "  transform_mode = vectorized"
  echo "  buffer_mb = 512"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "100000" \
    --writer_threads "16" \
    --commit_interval "50000" \
    --transform_mode "vectorized" \
    --buffer_mb "512" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  batch_size = 1000"
  echo "  writer_threads = 16"
  echo "  commit_interval = 1000"
  echo "  transform_mode = row"
  echo "  buffer_mb = 512"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "1000" \
    --writer_threads "16" \
    --commit_interval "1000" \
    --transform_mode "row" \
    --buffer_mb "512" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  batch_size = 100000"
  echo "  writer_threads = 1"
  echo "  commit_interval = 50000"
  echo "  transform_mode = row"
  echo "  buffer_mb = 512"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "100000" \
    --writer_threads "1" \
    --commit_interval "50000" \
    --transform_mode "row" \
    --buffer_mb "512" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  batch_size = 1000"
  echo "  writer_threads = 1"
  echo "  commit_interval = 1000"
  echo "  transform_mode = vectorized"
  echo "  buffer_mb = 512"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "1000" \
    --writer_threads "1" \
    --commit_interval "1000" \
    --transform_mode "vectorized" \
    --buffer_mb "512" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  batch_size = 1000"
  echo "  writer_threads = 1"
  echo "  commit_interval = 50000"
  echo "  transform_mode = vectorized"
  echo "  buffer_mb = 64"


  if ! "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/sim.sh" \
    --batch_size "1000" \
    --writer_threads "1" \
    --commit_interval "50000" \
    --transform_mode "vectorized" \
    --buffer_mb "64" \
    --source "postgresql" \
    --sink "s3_parquet" \
    --out "/workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/40_etl_batch_size_tuning/results"
