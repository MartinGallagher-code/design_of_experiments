#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:51:28.719736+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: API Rate Limiter Tuning

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  requests_per_sec = 100"
  echo "  burst_size = 100"
  echo "  window_type = fixed"
  echo "  penalty_duration = 10"
  echo "  global_limit = 5000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "100" \
    --burst_size "100" \
    --window_type "fixed" \
    --penalty_duration "10" \
    --global_limit "5000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  requests_per_sec = 1000"
  echo "  burst_size = 10"
  echo "  window_type = sliding"
  echo "  penalty_duration = 10"
  echo "  global_limit = 5000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "1000" \
    --burst_size "10" \
    --window_type "sliding" \
    --penalty_duration "10" \
    --global_limit "5000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  requests_per_sec = 1000"
  echo "  burst_size = 100"
  echo "  window_type = sliding"
  echo "  penalty_duration = 300"
  echo "  global_limit = 5000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "1000" \
    --burst_size "100" \
    --window_type "sliding" \
    --penalty_duration "300" \
    --global_limit "5000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  requests_per_sec = 1000"
  echo "  burst_size = 100"
  echo "  window_type = fixed"
  echo "  penalty_duration = 300"
  echo "  global_limit = 50000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "1000" \
    --burst_size "100" \
    --window_type "fixed" \
    --penalty_duration "300" \
    --global_limit "50000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  requests_per_sec = 100"
  echo "  burst_size = 100"
  echo "  window_type = sliding"
  echo "  penalty_duration = 10"
  echo "  global_limit = 50000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "100" \
    --burst_size "100" \
    --window_type "sliding" \
    --penalty_duration "10" \
    --global_limit "50000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  requests_per_sec = 1000"
  echo "  burst_size = 10"
  echo "  window_type = fixed"
  echo "  penalty_duration = 10"
  echo "  global_limit = 50000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "1000" \
    --burst_size "10" \
    --window_type "fixed" \
    --penalty_duration "10" \
    --global_limit "50000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  requests_per_sec = 100"
  echo "  burst_size = 10"
  echo "  window_type = sliding"
  echo "  penalty_duration = 300"
  echo "  global_limit = 50000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "100" \
    --burst_size "10" \
    --window_type "sliding" \
    --penalty_duration "300" \
    --global_limit "50000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  requests_per_sec = 100"
  echo "  burst_size = 10"
  echo "  window_type = fixed"
  echo "  penalty_duration = 300"
  echo "  global_limit = 5000"


  if ! "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/sim.sh" \
    --requests_per_sec "100" \
    --burst_size "10" \
    --window_type "fixed" \
    --penalty_duration "300" \
    --global_limit "5000" \
    --backend_capacity "20000" \
    --cache_backend "redis" \
    --out "/workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/33_api_rate_limiter/results"
