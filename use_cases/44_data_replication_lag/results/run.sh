#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:54:29.017423+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Data Replication Lag

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  sync_mode = async"
  echo "  binlog_batch_size = 100"
  echo "  parallel_workers = 16"
  echo "  network_buffer_kb = 64"
  echo "  compression = off"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "async" \
    --binlog_batch_size "100" \
    --parallel_workers "16" \
    --network_buffer_kb "64" \
    --compression "off" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  sync_mode = semi_sync"
  echo "  binlog_batch_size = 1"
  echo "  parallel_workers = 1"
  echo "  network_buffer_kb = 64"
  echo "  compression = off"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "semi_sync" \
    --binlog_batch_size "1" \
    --parallel_workers "1" \
    --network_buffer_kb "64" \
    --compression "off" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  sync_mode = semi_sync"
  echo "  binlog_batch_size = 100"
  echo "  parallel_workers = 1"
  echo "  network_buffer_kb = 1024"
  echo "  compression = off"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "semi_sync" \
    --binlog_batch_size "100" \
    --parallel_workers "1" \
    --network_buffer_kb "1024" \
    --compression "off" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  sync_mode = semi_sync"
  echo "  binlog_batch_size = 100"
  echo "  parallel_workers = 16"
  echo "  network_buffer_kb = 1024"
  echo "  compression = on"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "semi_sync" \
    --binlog_batch_size "100" \
    --parallel_workers "16" \
    --network_buffer_kb "1024" \
    --compression "on" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  sync_mode = async"
  echo "  binlog_batch_size = 100"
  echo "  parallel_workers = 1"
  echo "  network_buffer_kb = 64"
  echo "  compression = on"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "async" \
    --binlog_batch_size "100" \
    --parallel_workers "1" \
    --network_buffer_kb "64" \
    --compression "on" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  sync_mode = semi_sync"
  echo "  binlog_batch_size = 1"
  echo "  parallel_workers = 16"
  echo "  network_buffer_kb = 64"
  echo "  compression = on"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "semi_sync" \
    --binlog_batch_size "1" \
    --parallel_workers "16" \
    --network_buffer_kb "64" \
    --compression "on" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  sync_mode = async"
  echo "  binlog_batch_size = 1"
  echo "  parallel_workers = 1"
  echo "  network_buffer_kb = 1024"
  echo "  compression = on"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "async" \
    --binlog_batch_size "1" \
    --parallel_workers "1" \
    --network_buffer_kb "1024" \
    --compression "on" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  sync_mode = async"
  echo "  binlog_batch_size = 1"
  echo "  parallel_workers = 16"
  echo "  network_buffer_kb = 1024"
  echo "  compression = off"


  if ! "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/sim.sh" \
    --sync_mode "async" \
    --binlog_batch_size "1" \
    --parallel_workers "16" \
    --network_buffer_kb "1024" \
    --compression "off" \
    --engine "mysql_8" \
    --gtid "on" \
    --out "/workspaces/design_of_experiments/use_cases/44_data_replication_lag/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/44_data_replication_lag/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/44_data_replication_lag/results"
