#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:57:53.495930+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: WiFi Channel & Power

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  channel_width = 20"
  echo "  tx_power = 23"
  echo "  guard_interval = long"
  echo "  beamforming = off"
  echo "  spatial_streams = 1"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "20" \
    --tx_power "23" \
    --guard_interval "long" \
    --beamforming "off" \
    --spatial_streams "1" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  channel_width = 80"
  echo "  tx_power = 10"
  echo "  guard_interval = short"
  echo "  beamforming = off"
  echo "  spatial_streams = 1"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "80" \
    --tx_power "10" \
    --guard_interval "short" \
    --beamforming "off" \
    --spatial_streams "1" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  channel_width = 80"
  echo "  tx_power = 23"
  echo "  guard_interval = short"
  echo "  beamforming = on"
  echo "  spatial_streams = 1"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "80" \
    --tx_power "23" \
    --guard_interval "short" \
    --beamforming "on" \
    --spatial_streams "1" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  channel_width = 80"
  echo "  tx_power = 23"
  echo "  guard_interval = long"
  echo "  beamforming = on"
  echo "  spatial_streams = 4"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "80" \
    --tx_power "23" \
    --guard_interval "long" \
    --beamforming "on" \
    --spatial_streams "4" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  channel_width = 20"
  echo "  tx_power = 23"
  echo "  guard_interval = short"
  echo "  beamforming = off"
  echo "  spatial_streams = 4"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "20" \
    --tx_power "23" \
    --guard_interval "short" \
    --beamforming "off" \
    --spatial_streams "4" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  channel_width = 80"
  echo "  tx_power = 10"
  echo "  guard_interval = long"
  echo "  beamforming = off"
  echo "  spatial_streams = 4"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "80" \
    --tx_power "10" \
    --guard_interval "long" \
    --beamforming "off" \
    --spatial_streams "4" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  channel_width = 20"
  echo "  tx_power = 10"
  echo "  guard_interval = short"
  echo "  beamforming = on"
  echo "  spatial_streams = 4"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "20" \
    --tx_power "10" \
    --guard_interval "short" \
    --beamforming "on" \
    --spatial_streams "4" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  channel_width = 20"
  echo "  tx_power = 10"
  echo "  guard_interval = long"
  echo "  beamforming = on"
  echo "  spatial_streams = 1"


  if ! "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/sim.sh" \
    --channel_width "20" \
    --tx_power "10" \
    --guard_interval "long" \
    --beamforming "on" \
    --spatial_streams "1" \
    --standard "wifi6" \
    --band "5GHz" \
    --out "/workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/56_wifi_channel_power/results"
