#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:51:44.358023+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Audit Log Pipeline

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  batch_size = 100"
  echo "  flush_interval_ms = 5000"
  echo "  compression_level = 9"
  echo "  buffer_pool_mb = 32"
  echo "  writer_threads = 1"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "100" \
    --flush_interval_ms "5000" \
    --compression_level "9" \
    --buffer_pool_mb "32" \
    --writer_threads "1" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  batch_size = 10000"
  echo "  flush_interval_ms = 100"
  echo "  compression_level = 1"
  echo "  buffer_pool_mb = 32"
  echo "  writer_threads = 1"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "10000" \
    --flush_interval_ms "100" \
    --compression_level "1" \
    --buffer_pool_mb "32" \
    --writer_threads "1" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  batch_size = 10000"
  echo "  flush_interval_ms = 5000"
  echo "  compression_level = 1"
  echo "  buffer_pool_mb = 512"
  echo "  writer_threads = 1"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "10000" \
    --flush_interval_ms "5000" \
    --compression_level "1" \
    --buffer_pool_mb "512" \
    --writer_threads "1" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  batch_size = 10000"
  echo "  flush_interval_ms = 5000"
  echo "  compression_level = 9"
  echo "  buffer_pool_mb = 512"
  echo "  writer_threads = 8"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "10000" \
    --flush_interval_ms "5000" \
    --compression_level "9" \
    --buffer_pool_mb "512" \
    --writer_threads "8" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  batch_size = 100"
  echo "  flush_interval_ms = 5000"
  echo "  compression_level = 1"
  echo "  buffer_pool_mb = 32"
  echo "  writer_threads = 8"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "100" \
    --flush_interval_ms "5000" \
    --compression_level "1" \
    --buffer_pool_mb "32" \
    --writer_threads "8" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  batch_size = 10000"
  echo "  flush_interval_ms = 100"
  echo "  compression_level = 9"
  echo "  buffer_pool_mb = 32"
  echo "  writer_threads = 8"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "10000" \
    --flush_interval_ms "100" \
    --compression_level "9" \
    --buffer_pool_mb "32" \
    --writer_threads "8" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  batch_size = 100"
  echo "  flush_interval_ms = 100"
  echo "  compression_level = 1"
  echo "  buffer_pool_mb = 512"
  echo "  writer_threads = 8"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "100" \
    --flush_interval_ms "100" \
    --compression_level "1" \
    --buffer_pool_mb "512" \
    --writer_threads "8" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  batch_size = 100"
  echo "  flush_interval_ms = 100"
  echo "  compression_level = 9"
  echo "  buffer_pool_mb = 512"
  echo "  writer_threads = 1"


  if ! "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/sim.sh" \
    --batch_size "100" \
    --flush_interval_ms "100" \
    --compression_level "9" \
    --buffer_pool_mb "512" \
    --writer_threads "1" \
    --storage "s3" \
    --format "json_lines" \
    --out "/workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/65_audit_log_pipeline/results"
