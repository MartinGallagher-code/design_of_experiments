#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T17:38:42.695101+00:00
# Operation: fractional_factorial
# Total runs: 8
# Plan: Headphone EQ Calibration

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  bass_boost_db = 0"
  echo "  mid_presence_db = 3"
  echo "  treble_rolloff_db = 0"
  echo "  soundstage_pct = 0"
  echo "  crossfeed_pct = 0"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "0" \
    --mid_presence_db "3" \
    --treble_rolloff_db "0" \
    --soundstage_pct "0" \
    --crossfeed_pct "0" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  bass_boost_db = 8"
  echo "  mid_presence_db = -3"
  echo "  treble_rolloff_db = -6"
  echo "  soundstage_pct = 0"
  echo "  crossfeed_pct = 0"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "8" \
    --mid_presence_db "-3" \
    --treble_rolloff_db "-6" \
    --soundstage_pct "0" \
    --crossfeed_pct "0" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  bass_boost_db = 8"
  echo "  mid_presence_db = 3"
  echo "  treble_rolloff_db = -6"
  echo "  soundstage_pct = 100"
  echo "  crossfeed_pct = 0"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "8" \
    --mid_presence_db "3" \
    --treble_rolloff_db "-6" \
    --soundstage_pct "100" \
    --crossfeed_pct "0" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  bass_boost_db = 8"
  echo "  mid_presence_db = 3"
  echo "  treble_rolloff_db = 0"
  echo "  soundstage_pct = 100"
  echo "  crossfeed_pct = 60"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "8" \
    --mid_presence_db "3" \
    --treble_rolloff_db "0" \
    --soundstage_pct "100" \
    --crossfeed_pct "60" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  bass_boost_db = 0"
  echo "  mid_presence_db = 3"
  echo "  treble_rolloff_db = -6"
  echo "  soundstage_pct = 0"
  echo "  crossfeed_pct = 60"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "0" \
    --mid_presence_db "3" \
    --treble_rolloff_db "-6" \
    --soundstage_pct "0" \
    --crossfeed_pct "60" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  bass_boost_db = 8"
  echo "  mid_presence_db = -3"
  echo "  treble_rolloff_db = 0"
  echo "  soundstage_pct = 0"
  echo "  crossfeed_pct = 60"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "8" \
    --mid_presence_db "-3" \
    --treble_rolloff_db "0" \
    --soundstage_pct "0" \
    --crossfeed_pct "60" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  bass_boost_db = 0"
  echo "  mid_presence_db = -3"
  echo "  treble_rolloff_db = -6"
  echo "  soundstage_pct = 100"
  echo "  crossfeed_pct = 60"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "0" \
    --mid_presence_db "-3" \
    --treble_rolloff_db "-6" \
    --soundstage_pct "100" \
    --crossfeed_pct "60" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  bass_boost_db = 0"
  echo "  mid_presence_db = -3"
  echo "  treble_rolloff_db = 0"
  echo "  soundstage_pct = 100"
  echo "  crossfeed_pct = 0"


  if ! "/workspaces/design_of_experiments/use_cases/164_headphone_eq/sim.sh" \
    --bass_boost_db "0" \
    --mid_presence_db "-3" \
    --treble_rolloff_db "0" \
    --soundstage_pct "100" \
    --crossfeed_pct "0" \
    --headphone "open_back" \
    --source "lossless" \
    --out "/workspaces/design_of_experiments/use_cases/164_headphone_eq/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/164_headphone_eq/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/164_headphone_eq/results"
