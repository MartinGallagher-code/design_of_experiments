#!/usr/bin/env bash
# Generated by doe.py on 2026-03-24T21:52:08.465751+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: Service Mesh Sidecar

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  concurrency = 8"
  echo "  circuit_breaker_max = 10000"
  echo "  access_log = on"
  echo "  tracing_sample_pct = 1"
  echo "  compression = off"
  echo "  connection_pool = 100"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "8" \
    --circuit_breaker_max "10000" \
    --access_log "on" \
    --tracing_sample_pct "1" \
    --compression "off" \
    --connection_pool "100" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  concurrency = 1"
  echo "  circuit_breaker_max = 100"
  echo "  access_log = on"
  echo "  tracing_sample_pct = 100"
  echo "  compression = off"
  echo "  connection_pool = 100"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "1" \
    --circuit_breaker_max "100" \
    --access_log "on" \
    --tracing_sample_pct "100" \
    --compression "off" \
    --connection_pool "100" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  concurrency = 1"
  echo "  circuit_breaker_max = 10000"
  echo "  access_log = off"
  echo "  tracing_sample_pct = 100"
  echo "  compression = off"
  echo "  connection_pool = 1000"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "1" \
    --circuit_breaker_max "10000" \
    --access_log "off" \
    --tracing_sample_pct "100" \
    --compression "off" \
    --connection_pool "1000" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  concurrency = 8"
  echo "  circuit_breaker_max = 10000"
  echo "  access_log = on"
  echo "  tracing_sample_pct = 100"
  echo "  compression = on"
  echo "  connection_pool = 1000"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "8" \
    --circuit_breaker_max "10000" \
    --access_log "on" \
    --tracing_sample_pct "100" \
    --compression "on" \
    --connection_pool "1000" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  concurrency = 1"
  echo "  circuit_breaker_max = 10000"
  echo "  access_log = off"
  echo "  tracing_sample_pct = 1"
  echo "  compression = on"
  echo "  connection_pool = 100"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "1" \
    --circuit_breaker_max "10000" \
    --access_log "off" \
    --tracing_sample_pct "1" \
    --compression "on" \
    --connection_pool "100" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  concurrency = 8"
  echo "  circuit_breaker_max = 100"
  echo "  access_log = off"
  echo "  tracing_sample_pct = 100"
  echo "  compression = on"
  echo "  connection_pool = 100"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "8" \
    --circuit_breaker_max "100" \
    --access_log "off" \
    --tracing_sample_pct "100" \
    --compression "on" \
    --connection_pool "100" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  concurrency = 1"
  echo "  circuit_breaker_max = 100"
  echo "  access_log = on"
  echo "  tracing_sample_pct = 1"
  echo "  compression = on"
  echo "  connection_pool = 1000"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "1" \
    --circuit_breaker_max "100" \
    --access_log "on" \
    --tracing_sample_pct "1" \
    --compression "on" \
    --connection_pool "1000" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  concurrency = 8"
  echo "  circuit_breaker_max = 100"
  echo "  access_log = off"
  echo "  tracing_sample_pct = 1"
  echo "  compression = off"
  echo "  connection_pool = 1000"


  if ! "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/sim.sh" \
    --concurrency "8" \
    --circuit_breaker_max "100" \
    --access_log "off" \
    --tracing_sample_pct "1" \
    --compression "off" \
    --connection_pool "1000" \
    --mesh "istio" \
    --protocol "http2" \
    --out "/workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/35_service_mesh_sidecar/results"
