#!/usr/bin/env bash
# Generated by doe.py on 2026-03-25T01:49:39.800995+00:00
# Operation: plackett_burman
# Total runs: 8
# Plan: WAF Rule Threshold Tuning

set -uo pipefail

mkdir -p "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results"

FAILED_RUNS=()

echo "Starting 8 experiment runs..."
echo ""


if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_1.json" ]]; then
  echo "=== Run 1 / 8 already complete, skipping ==="
else
  echo "=== Run 1 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 10000"
  echo "  body_inspection_depth = 65536"
  echo "  anomaly_score_threshold = 15"
  echo "  paranoia_level = 1"
  echo "  sql_injection_sensitivity = 1"
  echo "  xss_detection_level = 1"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "10000" \
    --body_inspection_depth "65536" \
    --anomaly_score_threshold "15" \
    --paranoia_level "1" \
    --sql_injection_sensitivity "1" \
    --xss_detection_level "1" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_1.json"; then
    echo "ERROR: Run 1 failed"
    FAILED_RUNS+=(1)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_2.json" ]]; then
  echo "=== Run 2 / 8 already complete, skipping ==="
else
  echo "=== Run 2 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 100"
  echo "  body_inspection_depth = 1000"
  echo "  anomaly_score_threshold = 15"
  echo "  paranoia_level = 4"
  echo "  sql_injection_sensitivity = 1"
  echo "  xss_detection_level = 1"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "100" \
    --body_inspection_depth "1000" \
    --anomaly_score_threshold "15" \
    --paranoia_level "4" \
    --sql_injection_sensitivity "1" \
    --xss_detection_level "1" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_2.json"; then
    echo "ERROR: Run 2 failed"
    FAILED_RUNS+=(2)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_3.json" ]]; then
  echo "=== Run 3 / 8 already complete, skipping ==="
else
  echo "=== Run 3 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 100"
  echo "  body_inspection_depth = 65536"
  echo "  anomaly_score_threshold = 3"
  echo "  paranoia_level = 4"
  echo "  sql_injection_sensitivity = 1"
  echo "  xss_detection_level = 5"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "100" \
    --body_inspection_depth "65536" \
    --anomaly_score_threshold "3" \
    --paranoia_level "4" \
    --sql_injection_sensitivity "1" \
    --xss_detection_level "5" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_3.json"; then
    echo "ERROR: Run 3 failed"
    FAILED_RUNS+=(3)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_4.json" ]]; then
  echo "=== Run 4 / 8 already complete, skipping ==="
else
  echo "=== Run 4 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 10000"
  echo "  body_inspection_depth = 65536"
  echo "  anomaly_score_threshold = 15"
  echo "  paranoia_level = 4"
  echo "  sql_injection_sensitivity = 9"
  echo "  xss_detection_level = 5"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "10000" \
    --body_inspection_depth "65536" \
    --anomaly_score_threshold "15" \
    --paranoia_level "4" \
    --sql_injection_sensitivity "9" \
    --xss_detection_level "5" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_4.json"; then
    echo "ERROR: Run 4 failed"
    FAILED_RUNS+=(4)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_5.json" ]]; then
  echo "=== Run 5 / 8 already complete, skipping ==="
else
  echo "=== Run 5 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 100"
  echo "  body_inspection_depth = 65536"
  echo "  anomaly_score_threshold = 3"
  echo "  paranoia_level = 1"
  echo "  sql_injection_sensitivity = 9"
  echo "  xss_detection_level = 1"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "100" \
    --body_inspection_depth "65536" \
    --anomaly_score_threshold "3" \
    --paranoia_level "1" \
    --sql_injection_sensitivity "9" \
    --xss_detection_level "1" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_5.json"; then
    echo "ERROR: Run 5 failed"
    FAILED_RUNS+=(5)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_6.json" ]]; then
  echo "=== Run 6 / 8 already complete, skipping ==="
else
  echo "=== Run 6 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 10000"
  echo "  body_inspection_depth = 1000"
  echo "  anomaly_score_threshold = 3"
  echo "  paranoia_level = 4"
  echo "  sql_injection_sensitivity = 9"
  echo "  xss_detection_level = 1"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "10000" \
    --body_inspection_depth "1000" \
    --anomaly_score_threshold "3" \
    --paranoia_level "4" \
    --sql_injection_sensitivity "9" \
    --xss_detection_level "1" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_6.json"; then
    echo "ERROR: Run 6 failed"
    FAILED_RUNS+=(6)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_7.json" ]]; then
  echo "=== Run 7 / 8 already complete, skipping ==="
else
  echo "=== Run 7 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 100"
  echo "  body_inspection_depth = 1000"
  echo "  anomaly_score_threshold = 15"
  echo "  paranoia_level = 1"
  echo "  sql_injection_sensitivity = 9"
  echo "  xss_detection_level = 5"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "100" \
    --body_inspection_depth "1000" \
    --anomaly_score_threshold "15" \
    --paranoia_level "1" \
    --sql_injection_sensitivity "9" \
    --xss_detection_level "5" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_7.json"; then
    echo "ERROR: Run 7 failed"
    FAILED_RUNS+=(7)
  fi

fi

echo ""

if [[ -f "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_8.json" ]]; then
  echo "=== Run 8 / 8 already complete, skipping ==="
else
  echo "=== Run 8 / 8 (Block 1) ==="
  echo "  rate_limit_rps = 10000"
  echo "  body_inspection_depth = 1000"
  echo "  anomaly_score_threshold = 3"
  echo "  paranoia_level = 1"
  echo "  sql_injection_sensitivity = 1"
  echo "  xss_detection_level = 5"


  if ! "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/sim.sh" \
    --rate_limit_rps "10000" \
    --body_inspection_depth "1000" \
    --anomaly_score_threshold "3" \
    --paranoia_level "1" \
    --sql_injection_sensitivity "1" \
    --xss_detection_level "5" \
    --waf_engine "modsecurity" \
    --ruleset "owasp_crs" \
    --out "/workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results/run_8.json"; then
    echo "ERROR: Run 8 failed"
    FAILED_RUNS+=(8)
  fi

fi

echo ""

if [[ ${#FAILED_RUNS[@]} -gt 0 ]]; then
  echo "WARNING: ${#FAILED_RUNS[@]} run(s) failed: ${FAILED_RUNS[*]}"
  echo "Results (partial) in: /workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results"
  exit 1
fi

echo "All runs complete. Results in: /workspaces/design_of_experiments/use_cases/57_waf_rule_threshold/results"
