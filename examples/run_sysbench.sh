#!/usr/bin/env bash
# Generated by doe.py on 2026-03-18T21:23:21.358009+00:00
# Operation: plackett_burman
# Total runs: 16

set -euo pipefail

mkdir -p "results/sysbench"

echo "Starting 16 experiment runs..."
echo ""


if [[ -f "results/sysbench/run_1.json" ]]; then
  echo "=== Run 1 / 16 already complete, skipping ==="
else
  echo "=== Run 1 / 16 (Block 1) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = read"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "16K" \
    --memory_scope_type "global" \
    --memory_access_mode "rnd" \
    --memory_oper "read" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_1.json"
fi

echo ""

if [[ -f "results/sysbench/run_2.json" ]]; then
  echo "=== Run 2 / 16 already complete, skipping ==="
else
  echo "=== Run 2 / 16 (Block 1) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = write"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "16K" \
    --memory_scope_type "local" \
    --memory_access_mode "rnd" \
    --memory_oper "write" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_2.json"
fi

echo ""

if [[ -f "results/sysbench/run_3.json" ]]; then
  echo "=== Run 3 / 16 already complete, skipping ==="
else
  echo "=== Run 3 / 16 (Block 1) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = write"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "16K" \
    --memory_scope_type "global" \
    --memory_access_mode "seq" \
    --memory_oper "write" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_3.json"
fi

echo ""

if [[ -f "results/sysbench/run_4.json" ]]; then
  echo "=== Run 4 / 16 already complete, skipping ==="
else
  echo "=== Run 4 / 16 (Block 1) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = read"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "1K" \
    --memory_scope_type "local" \
    --memory_access_mode "rnd" \
    --memory_oper "read" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_4.json"
fi

echo ""

if [[ -f "results/sysbench/run_5.json" ]]; then
  echo "=== Run 5 / 16 already complete, skipping ==="
else
  echo "=== Run 5 / 16 (Block 1) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = write"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "1K" \
    --memory_scope_type "local" \
    --memory_access_mode "seq" \
    --memory_oper "write" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_5.json"
fi

echo ""

if [[ -f "results/sysbench/run_6.json" ]]; then
  echo "=== Run 6 / 16 already complete, skipping ==="
else
  echo "=== Run 6 / 16 (Block 1) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = read"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "16K" \
    --memory_scope_type "local" \
    --memory_access_mode "seq" \
    --memory_oper "read" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_6.json"
fi

echo ""

if [[ -f "results/sysbench/run_7.json" ]]; then
  echo "=== Run 7 / 16 already complete, skipping ==="
else
  echo "=== Run 7 / 16 (Block 1) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = read"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "1K" \
    --memory_scope_type "global" \
    --memory_access_mode "seq" \
    --memory_oper "read" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_7.json"
fi

echo ""

if [[ -f "results/sysbench/run_8.json" ]]; then
  echo "=== Run 8 / 16 already complete, skipping ==="
else
  echo "=== Run 8 / 16 (Block 1) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = write"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "1K" \
    --memory_scope_type "global" \
    --memory_access_mode "rnd" \
    --memory_oper "write" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_8.json"
fi

echo ""

if [[ -f "results/sysbench/run_9.json" ]]; then
  echo "=== Run 9 / 16 already complete, skipping ==="
else
  echo "=== Run 9 / 16 (Block 2) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = read"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "16K" \
    --memory_scope_type "local" \
    --memory_access_mode "seq" \
    --memory_oper "read" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_9.json"
fi

echo ""

if [[ -f "results/sysbench/run_10.json" ]]; then
  echo "=== Run 10 / 16 already complete, skipping ==="
else
  echo "=== Run 10 / 16 (Block 2) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = write"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "16K" \
    --memory_scope_type "global" \
    --memory_access_mode "seq" \
    --memory_oper "write" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_10.json"
fi

echo ""

if [[ -f "results/sysbench/run_11.json" ]]; then
  echo "=== Run 11 / 16 already complete, skipping ==="
else
  echo "=== Run 11 / 16 (Block 2) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = write"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "16K" \
    --memory_scope_type "local" \
    --memory_access_mode "rnd" \
    --memory_oper "write" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_11.json"
fi

echo ""

if [[ -f "results/sysbench/run_12.json" ]]; then
  echo "=== Run 12 / 16 already complete, skipping ==="
else
  echo "=== Run 12 / 16 (Block 2) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = read"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "1K" \
    --memory_scope_type "global" \
    --memory_access_mode "seq" \
    --memory_oper "read" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_12.json"
fi

echo ""

if [[ -f "results/sysbench/run_13.json" ]]; then
  echo "=== Run 13 / 16 already complete, skipping ==="
else
  echo "=== Run 13 / 16 (Block 2) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 16K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = read"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "16K" \
    --memory_scope_type "global" \
    --memory_access_mode "rnd" \
    --memory_oper "read" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_13.json"
fi

echo ""

if [[ -f "results/sysbench/run_14.json" ]]; then
  echo "=== Run 14 / 16 already complete, skipping ==="
else
  echo "=== Run 14 / 16 (Block 2) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = seq"
  echo "  memory_oper = write"
  echo "  rand_type = uniform"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "1K" \
    --memory_scope_type "local" \
    --memory_access_mode "seq" \
    --memory_oper "write" \
    --rand_type "uniform" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_14.json"
fi

echo ""

if [[ -f "results/sysbench/run_15.json" ]]; then
  echo "=== Run 15 / 16 already complete, skipping ==="
else
  echo "=== Run 15 / 16 (Block 2) ==="
  echo "  threads = 1"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = local"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = read"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "1" \
    --memory_block_size "1K" \
    --memory_scope_type "local" \
    --memory_access_mode "rnd" \
    --memory_oper "read" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_15.json"
fi

echo ""

if [[ -f "results/sysbench/run_16.json" ]]; then
  echo "=== Run 16 / 16 already complete, skipping ==="
else
  echo "=== Run 16 / 16 (Block 2) ==="
  echo "  threads = 4"
  echo "  memory_block_size = 1K"
  echo "  memory_scope_type = global"
  echo "  memory_access_mode = rnd"
  echo "  memory_oper = write"
  echo "  rand_type = special"

  "examples/sysbench_test.sh" \
    --threads "4" \
    --memory_block_size "1K" \
    --memory_scope_type "global" \
    --memory_access_mode "rnd" \
    --memory_oper "write" \
    --rand_type "special" \
    --time=10 \
    --warmup-time=3 \
    --out "results/sysbench/run_16.json"
fi

echo ""

echo "All runs complete. Results in: results/sysbench"
